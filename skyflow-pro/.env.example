# Backend URL (must match backend-server port in application.yml)
VITE_API_BASE_URL=http://localhost:8081

# Set to true to use mock flight data instead of backend (e.g. when backend is not running)
VITE_USE_MOCKS=false
